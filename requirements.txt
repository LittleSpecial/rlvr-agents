# RLVR Agents - Requirements
# Python >= 3.8 (some clusters only provide cp38 CUDA PyTorch wheels)

# Core (always needed)
numpy>=1.24.0,<2.0  # PyTorch 1.13 不兼容 NumPy 2.x
pyyaml>=6.0

# For real LLM training (HF backend)
# 注意：集群 PyTorch 1.13.1+cu116，需要旧版本依赖
torch>=1.11.0
transformers>=4.30.0,<4.40.0  # 4.40+ 可能需要 torch 2.x
peft>=0.5.0,<0.7.0  # 0.7+ 需要 torch 2.0+，0.10+ 需要 torch 2.2+
datasets>=2.14.0,<3.0.0
# accelerate 和 trl 暂不使用

# For faster inference (optional)
# vllm>=0.2.7

# Experiment tracking (optional)
# wandb>=0.16.0
# tensorboard>=2.15.0

# Development
# pytest>=7.4.0
# black>=23.0.0
